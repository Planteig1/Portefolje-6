// GET STORED USER
let currentUserId = localStorage.getItem("userId");
console.log(currentUserId)

// IF USER LOGGED IN CHANGE NAV BAR
if (currentUserId) {
    // REMOVE LOGIN / SIGNUP
    let navLoginATag = document.querySelector("")
}